# template

version: '2'

services:

    web:
      build: .
      command: python route.py
      environment:
        - DEBUG=False
        - MONGO_URL=mongodb://db:27017

    db:
      image: mongo
      command: mongod --smallfiles
