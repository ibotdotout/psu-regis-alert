web:
  build: .
  command: python route.py
  volumes:
   - .:/app
  links:
    - db:db
  ports:
   - "5000:5000"

mornitor:
  build: .
  command: ./bin/cron
  volumes:
   - .:/app
  links:
    - db:db

db:
  image: dockerfile/mongodb
  command: mongod --smallfiles
  ports:
   - "27017:27017"
