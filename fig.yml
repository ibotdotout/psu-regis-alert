web:
  build: .
  command: ./bin/web
  volumes:
   - .:/app
  links:
   - db
  ports:
   - "80:80"

db:
  image: dockerfile/mongodb
  command: mongod --smallfiles
  volumes:
   - ./db:/data/db
  ports:
   - "27017:27017"
