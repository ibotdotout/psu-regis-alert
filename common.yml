# template

web:
  build: .
  command: python route.py
  volumes:
   - .:/app
  ports:
   - "80:5000"
  environment:
    - DEBUG=False

db:
  image: mongo
  command: mongod --smallfiles
  expose:
   - "27017"
